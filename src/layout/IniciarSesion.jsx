import { Outlet } from 'react-router-dom'

const IniciarSesion = ({}) => {

    return(
        <div>
            <p>Iniciar Sesion...</p>
            <Outlet />
        </div>
    )
}

export default IniciarSesion